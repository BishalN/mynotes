import { extendTheme } from 'native-base';

export const theme = extendTheme({
  fonts: {
    heading: 'CarterOne_400Regular',
    body: 'CarterOne_400Regular',
    mono: 'CarterOne_400Regular',
  },
});
